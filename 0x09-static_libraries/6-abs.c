#include <stdio.h>
#include "main.h"
/**
 * _abs - find absolute value of an integer
 * @i: store int
 *
 * Return: 0
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
